1) What is difference between below statement ?

*)
	CREATE TABLE new_table AS
	SELECT *
	FROM old_table
	WHERE 1 = 0;

	*) It Copies the Column names, Data types
	*) It doesn't copy the Data,Indexes,Constraints

*)
	CREATE TABLE new_table LIKE old_table;

	*) It Copies the Column names, Data types, Indexes, Constraints
	*) It doesn't copy the Data, Foreign key relationships

#Note :
WHERE 1 = 0
This condition is always FALSE
So no rows are returned



2) What is a DEFAULT constraint in SQL?
	It assign a value to a column when no value is provided during an INSERT.