Note :
        1) Try to indentify the type of subquery (where we should apply the logic)

            *) Where
            *) Select
            *) From
            *) Having

        2) CTE:
            1) Modularity logics
